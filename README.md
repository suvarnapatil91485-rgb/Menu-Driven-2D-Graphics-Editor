# Menu-Driven-2D-Graphics-Editor
A menu-driven 2D Graphics Editor in C that uses a 2D character array as a canvas. Supports drawing lines, rectangles, triangles, and circles using `*`, with `_` as the background. Users can add, delete, modify, and display graphical objects through an interactive menu.
